{
  "name": "playwright_auth_many_users",
  "version": "1.0.0",
  "description": "This template project for login with many users, save the .auth to json then load it when running",
  "main": "index.js",
  "scripts": {},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "@types/node": "^20.12.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
